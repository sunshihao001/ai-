# Inbox

Drop raw links, copied snippets, or quick article notes here before normalization.

Every item that influences the method wheel should eventually become:

```text
sources/<source-id>.md
synthesis/<topic>.md
frame-updates/<proposal>.md or decisions/<decision>.md
```
